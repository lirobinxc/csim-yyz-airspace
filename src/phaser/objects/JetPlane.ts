import { AcModel } from '../types/AircraftTypes';
import Plane from './Plane/Plane';

export default class JetPlane {
  constructor(
    scene: Phaser.Scene,
    coord: Phaser.Math.Vector2,
    acModel: AcModel
  ) {}
}
